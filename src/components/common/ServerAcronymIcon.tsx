import { styled } from '@mui/material';

const ServerAcronymIcon = styled('div')({
	textAlign: 'center',
	lineHeight: '4rem',
	fontSize: '1.5rem',
	overflow: 'hidden',
});

export default ServerAcronymIcon;
